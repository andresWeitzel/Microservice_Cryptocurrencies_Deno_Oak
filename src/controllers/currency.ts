//External
import { getCurrencyType } from "../services/currency";

export const getCurrencyTypeController = async (req: Request, res:Response) => {
    try {
        console.log(req);
  
    } catch (error) {
      console.log(error);
    }
  };